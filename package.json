{
  "name": "time-in-the-bottle-shell",
  "private": true,
  "version": "0.1.0",
  "description": "Tauri shell for Time in the Bottle — loads the GitHub Pages bottle UI. The web UI itself stays vanilla JS with no build step (index.html).",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "party:dev": "wrangler dev",
    "party:deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "wrangler": "^4.101.0"
  }
}
