{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "menu-window",
  "description": "The menu popover window (remote #menu page): receive the roster via events, send user actions + exit back, hide itself, and resize itself to fit its content height (setSize). Positioning + click-away hiding are done natively (Rust).",
  "windows": ["menu"],
  "remote": {
    "urls": [
      "https://sandtogether.indiegames.design",
      "http://localhost:8090",
      "http://127.0.0.1:8090"
    ]
  },
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:event:allow-emit",
    "core:window:allow-hide",
    "core:window:allow-set-size"
  ]
}
